# 产品目标

系统接收自然语言研究问题，在固定预算内检索、去重并排序相关论文，输出可追溯结果和运行证据。

当前交付范围：

- replay 与显式授权的 live 两种运行模式；
- OpenAlex 与 Semantic Scholar 检索适配器；
- 词法融合、预算控制、失败降级与结构化诊断；
- 正式数据冻结、快照回放、评估与质量门；
- FastAPI 接口、命令行和浏览器界面；
- 可插拔的单模块召回优化框架。

不在当前稳定范围：历史查询演化、标题候选、嵌入排序、神经重排、一次性数据修复脚本及历史实验报告。
