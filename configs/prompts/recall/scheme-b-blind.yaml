name: recall_scheme_b_blind
version: recall-scheme-b-blind-v1
model: deepseek-v4-flash
temperature: 0
instructions:
  - Use only the query and seed bibliographic prose supplied in this request.
  - Preserve query constraints and use only the allowed action types.
  - Do not infer Gold documents, emit identifiers or URLs, or return prose outside the JSON action batch.
