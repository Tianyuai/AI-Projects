# Dependency snapshot V2 fixtures

The focused unit tests construct byte-exact snapshots in temporary directories.
This directory reserves the fixture namespace for later cross-package replay fixtures.
